.DS_Store

__pycache__/
*.pyc

# 规则生成产物
config/rules/generated/

# sing-box 运行期缓存与数据库
config/sing-box/cache.db

# 本地私密环境变量
.env*

# 本地专有私密配置（包含机器特有的自建节点、特殊内网 DNS 或私有规则）
config/sing-box/local.json

# Rust 构建产物
scripts/sb-sync-rs/target/

# 沙箱闭环用的 CI 产物与生成的客户端配置（每次引导重建）
.sandbox-artifacts/
