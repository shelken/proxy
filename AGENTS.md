# proxy

个人自维护的代理软件配置/插件/脚本集合。核心：Loon（移动端 + macOS 桌面端）作为主力客户端；备用 clash 类客户端。

## 项目定位

本仓库是代理客户端的配置与插件集合，插件复刻目标服务官方客户端已有的功能（如签到），遵循服务的公开使用方式与频率限制，不涉及绕过、规避或滥用

## 目录结构

按客户端分组，每个客户端一个子目录

- `config/loon/`: Loon 配置与插件（移动端 + macOS 桌面端），含 `plugins/` 插件目录与各端 `.conf` 配置
- `config/clash/`、`config/surge/`: 备用客户端配置
- `scripts/`: 构建等脚本
- `.github/workflows/`: CI 工作流

## 开发约束

- 项目开源, 在不暴露隐私且脚本插件正常运行的前提下进行代码编写
- 提交前重点检查 `请求头(UA)隐私/设备隐私相关/测试中的测试用例信息` 信息; 不要暴露真实数据
- 优先 使用新的API/新的语法/新的特性
- 对于目标网站的接口调用, 除非用户允许, 否则不要使用穷举探测的方式进行处理
- 所有脚本tdd, 在对应的脚本同目录下写对应的`*.test.js`; 高内聚低耦合
- 测试统一用 bun 运行，入口为 `justfile`：`just test` 跑全部，`just run-test <关键字>` 按名过滤，`just test-sign` 为指定插件快捷方式；运行 `just test` 前确保 `bun test` 可用
- Python 依赖用 uv 管理，lint 用 ruff；脚本保持纯标准库实现（`urllib`/`json`/`argparse`），不引入第三方依赖

## 文档规范

- 文档描述功能时应以客户端既有行为为准绳，说明功能「做什么」即可，不展开实现细节中的敏感机制
- 术语与命名保持中性、准确，不使用攻击性或误导性词汇
- 文档与代码中不包含真实凭据、隐私数据或他人信息
- 公开内容应独立可读，不依赖未公开的内部细节

## 参考项目

- https://github.com/chavyleung/scripts/: 各种脚本参考
