# 路由策略的先后即优先级，先匹配先胜 —— build_routeset 按这个顺序排 route.rules。
#
# 一行一个 policy，就是 index.txt 的 policy 列里的名字。未列出的策略不会丢，
# 只是按字母序排到已列出的之后（生成时会打印一条提示）。
#
# reject 是特殊项：它编译成 action: reject，不是出站。（从这里删掉它也一样会
# 被当成普通策略，因为 build_routeset 对 reject 的判断不看本文件。）

reject
gemini
openai
appleai
opencode
dev
ptcg
japansite
adultnsfw
direct
proxy
